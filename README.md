# AccidentsProject
Predict UK traffic accident severity (slight, serious, fatal) using 2018 datasets (Accidents, Casualties, Vehicles). Cleaned missing values, handled outliers, encoded categorical data, engineered date/time features, trained multiple ML models, compared performance, and visualized results with confusion matrices and ROC curves.
